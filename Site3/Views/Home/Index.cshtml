@{ ViewBag.Title = "Фирма 3"; }

@section navigation{

<ul class="nav nav-pills nav-stacked" id="sidebar">
    <li class="active">@Html.ActionLink("Прайс", "Index")</li>
    <li>@Html.ActionLink("О компании", "About")</li>
    <li>@Html.ActionLink("Контакты", "Info")</li>
</ul>
}

<h2 id="sec1">Прайс-лист</h2>
<p>
    Цены указаны с НДС при покупке более одной тонны.
    При покупке от 500 до 1000 кг наценка — 5% от оптовой цены, от 100 до 500 кг наценка — 10%.
</p>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Размер/Диаметр</th>
            <th>Сталь</th>
            <th>Цена</th>
            <th>Примечание</th>
        </tr>
    </thead>

    @foreach (var item in Model.Items)
    {
        <tr>
            <td>@item.Size</td>
            <td>@item.SteelMark</td>
            <td>@item.Price</td>
            <td>@item.Note</td>
        </tr>
    }
</table>
